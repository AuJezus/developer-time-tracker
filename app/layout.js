import { Roboto_Mono } from "next/font/google";
import "./globals.css";
import { cn } from "@/lib/utils";
import { Toaster } from "sonner";
import { BiLogoVenmo } from "react-icons/bi";

const fontMono = Roboto_Mono({ subsets: ["latin"], variable: "--font-mono" });

export const metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

const projects = [
  "developer-time-tracker",
  "personal-website",
  "skills-introduction-to-github",
  "skypark-redesign",
  "gym-tracker",
  "exrx-scraper",
  "car-events",
  "rate-my-link",
  "spotify-clone",
  "learn-next",
  "forkify",
  "todo-cli",
  "picvert",
  "page-text-extractor",
  "aujezus-play",
  "writingsdev",
  "pazusiu-bitynas",
];

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={cn(
          "min-h-screen font-mono antialiased dark",
          fontMono.variable
        )}
      >
        {/* <div className="grid grid-cols-[auto_1fr] transition-all grid-rows-[auto_1fr] w-full h-screen">
          <div className="row-span-2 hover:w-fit w-14 transition-all duration-1000">
            <div className="bg-green-500 sticky top-0 transition-all w-full h-screen flex flex-col gap-4 px-4 py-2">
              {projects.map((project) => (
                <div key={project}>{project}</div>
              ))}
            </div>
          </div>
          <div className="bg-purple-500">hi</div>
          <div className="bg-cyan-500 h-[1000px]">{children}</div>
        </div> */}

        {/* <div className="grid nav grid-rows-[auto_1fr] transition-all">
          <div className="row-span-2">
            <div className="bg-green-500 side-nav sticky top-0 h-screen flex flex-col gap-4 p-2">
              {projects.map((project) => (
                <div key={project} className="flex gap-2 items-center">
                  <BiLogoVenmo className="flex-shrink-0" />
                  <p className="text-nowrap overflow-hidden w-full overflow-ellipsis">
                    {project}
                  </p>
                </div>
              ))}
            </div>
          </div>
          <div className="bg-purple-500">hi</div>
          <div className="bg-cyan-500 h-[1000px]">{children}</div>
        </div> */}

        <div className="grid nav grid-rows-[auto_1fr] transition-all relative">
          <div className="row-span-2 bg-green-500 side-nav sticky top-0 h-screen flex flex-col gap-4 p-2">
            {projects.map((project) => (
              <div key={project} className="flex gap-2 items-center">
                <BiLogoVenmo className="flex-shrink-0" />
                <p className="text-nowrap overflow-hidden w-full overflow-ellipsis">
                  {project}
                </p>
              </div>
            ))}
          </div>
          <div className="bg-purple-500">hi</div>
          <div className="bg-cyan-500 h-[10000px]">{children}</div>
        </div>

        <Toaster richColors theme="dark" />
      </body>
    </html>
  );
}
